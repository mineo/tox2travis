language: python
cache: pip
dist: xenial
matrix:
  include:
  - python: "jython"
    env: TOXENV=flake8
  - python: "3.2"
    env: TOXENV=test
install:
  - travis_retry pip install tox
script:
  - travis_retry tox
